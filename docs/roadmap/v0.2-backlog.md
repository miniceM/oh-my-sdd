# oh-my-sdd v0.2 Backlog

来源：
- 实施过程中累积的 minor findings（task-scoped reviews + final review）
- 真实环境测试发现的问题（spec § 11 待核实项 + 实测爆雷）
- Claude Code 官方文档对齐（plugin reference + marketplaces）

> **最后更新**：2026-06-30（核对代码现状后刷新状态）

## 🔴 P0：v0.1 实测发现的核心 bug（全部已解决 ✅）

### B-1 baseline 注入实测不工作 ✅ 已解决（commit `ad1e5c9`）
**根因**：Claude Code plugin SessionStart hook 的 `additionalContext` 有已知 bug #16538（silently dropped）。
**最终方案**：install.js 写入 `~/.claude/CLAUDE.md`（用户级，每次会话自动加载），用 BEGIN/END marker 段实现幂等升级与卸载清理。
**遗留监控**：[issue #16538](https://github.com/anthropics/claude-code/issues/16538) 修复后可回归"纯 plugin"方案。

### B-2 npm postinstall 输出默认被吞 ✅ 已缓解
**现象**：`npm install -g @cli-tools/oh-my-sdd` 不加 `--foreground-scripts` 完全静默。
**现状**：已在 README 推荐 `--foreground-scripts` + `npm config set foreground-scripts true`。接受行业惯例。

## 🟡 P1：官方对齐与功能补全

### F-1 commands/ 迁移到 skills/ ✅ 完成（alpha 测试发现）
**触发**：用户在 demo 工程实测发现 `/oh-my-sdd:sdd-spec` 等 5 个命令带 plugin 前缀，与 `/security-check` 等无前缀 skills 不一致——用户体验割裂。
**已完成**：5 个命令从 `commands/` 迁到 `skills/<name>/SKILL.md`，plugin.json 移除 `commands` 字段，package.json `files` 数组移除 `commands/`。
**经验**：F-1 在 backlog 里本是"官方推荐方向"（nice-to-have），alpha 测试升级为 must-have。**证明 alpha 阶段价值——5 分钟用户测试比 1 小时静态审查更有效。**

### F-2 用 userConfig 替代手改 config.json
**状态**：❌ 未开始
**原因**：官方 plugin 提供 `userConfig` 字段，用户在 `/plugin` 里交互配置，不用手改 `~/.oh-my-sdd/config.json`。
**改造**：
```json
"userConfig": {
  "dop_endpoint": { "type": "string", "title": "DOP endpoint URL", "required": true },
  "aih_system_name": { "type": "string", "title": "AIH system 名", "default": "sdd" },
  "telemetry_disabled": { "type": "boolean", "title": "禁用遥测", "default": false }
}
```
hook 里读 `${user_config.dop_endpoint}` 替代 `loadConfig()`。敏感字段（如 DOP 鉴权）用 `"sensitive": true` 走 keychain。

### F-3 files_touched 真正上报到 session.end
**状态**：🔄 增量记录已实现，但 session-end 未聚合上报
**现状**：PostToolUse（`post-tool-use.js:69-70`）正确增量记录 `meta.files_touched[{path, count}]`，但 session-end.js 仅从 meta 读取 `slash_commands`，未读 `files_touched`。session.end 事件 schema 缺少此字段。
**待改造**：
- session-end.js 读 `meta.files_touched` 并聚合写入 session.end 事件
- DOP 后端配套改

### F-4 slash_commands 去重
**状态**：❌ 未开始
**现状**：`user-prompt-submit.js:54` 追加命令名到数组，`session-end.js:129` 直接取数组。多次调用同一命令会重复。
**改造**：session-end.js 聚合为 `{ cmd_name: count }` 格式。

### F-5 SessionEnd 不可靠场景补传 session.end
**状态**：⚠️ session-start.js 已有 orphan 清理（`cleanupOrphans`），但不补传 session.end
**现状**：`session-end.js:51-73` 有 `cleanupOrphans()`，仅删除过期 meta（>7天），不检测孤儿 session.start 也不补传。session-start.js 也不检查是否有上次未关闭的 session。
**待改造**：
- session-start.js 检测孤儿 session meta（有 `start_sha` 但没有对应的 session.end 记录）
- 补传 session.end（用 PostToolUse 增量记录的 files_touched 推断 code_delta）
- 标 `recovered: true` 字段

### F-6 checkNodeVersion 单段输入 NaN bug
**状态**：❌ 未修复
**位置**：`hooks/lib/platform.js:13-21`
**问题**：`checkNodeVersion('18')` 会 NaN 比较，单段版本号（如 `'18'` → `[18]`）只用 index 0 比较，循环到 index 1/2 时 `current[1]` vs `NaN` 比较结果为 false 但仍可能错误通过。
**实际影响**：install.js 调用 `checkNodeVersion('18.0.0')` 是 3 段，不受影响。但 API 本身有 bug。

## 🟢 P2：实施过程累积的 minor cleanup

### C-1 删除 legacy cleanup 代码
**状态**：⚠️ 待 v0.2 发布时执行
**位置**：`uninstall.js` 的 `cleanupLegacyFiles` + `cleanupLegacySettings`
**原因**：v0.1 发布后，没用户有 pre-c753589 的旧 install 产物了。
**时间**：v0.2 发布时（给企业用户 1 个版本周期升级）。

### C-2 删除 plan-mandated 死代码
**状态**：❌ 未开始
- `log.js:5 LEVELS` 未使用（仅内部 write 使用 levelName 字符串，`LEVELS` 数组未被外部引用）
- 多个测试文件的 unused imports（existsSync / mkdirSync / readFileSync 等）
- `git-diff.js:34 endRef` dead param（已文档化为 intentional，session-end.js:117 传 `'HEAD'`）

### C-3 hooks/lib/log.js toStderr 参数清理
**状态**：❌ 未修复
**位置**：`log.js:14`
**问题**：`write(level, msg, toStderr = false)` 第三个参数没有任何 public 函数（debug/info/warn/error）传入，dead API。但内部逻辑 `toStderr || level === 'error' || level === 'warn'` 中 warn/error 自动走 stderr 是正确的。

### C-4 isIamInPath 命名/语义统一
**状态**：🔄 部分修复（session-start.js 已改用 sync，其他调用方仍 await）
**位置**：`platform.js:54`（函数定义是 sync）
**问题**：函数名暗示 async（`is...`），但实现是 sync（`execFileSync`）。
**现状**：
- ✅ `session-start.js:68` 已改为 sync 调用（`if (!isIamInPath())`）
- ❌ `oms-login.js:60` 仍 `await isIamInPath()`（无害但风格不一致）
- ❌ `__tests__/unit/platform.test.js:37` 仍 `await isIamInPath()`
**修复**：全部改为 sync 调用（去掉 await），或改为真正的 async 实现。

### C-5 token lint 改用 tiktoken
**状态**：❌ 未开始
**位置**：`scripts/check-baseline-tokens.mjs`
**现状**：启发式估算（中文/2 + 英文/0.75），~28% 误差。
**选项**：
- A. 保持启发式，文档注明"实际 token 数 ±30%"（已选）
- B. 引入 devDependency（不影响 runtime）
- C. 调用 Claude API 的 `count_tokens` 端点

### C-6 NEED_LOGIN 测试 regex 收紧
**状态**：❌ 未修复
**位置**：`__tests__/integration/session-start.test.js:154,231,232`
**现状**：`/iam 身份认证|未授权|认证状态/` 太宽，NEED_LOGIN 和 ERROR 都匹配。
**修复**：去掉 `认证状态`，或 session-start.js 给每个状态发不同 stderr 消息。

## 🔵 P3：战略级新功能

### S-1 显式 subagents（agents/）
**状态**：❌ 未开始
**原因**：spec § 12 v0.2 计划项。当前无 `agents/` 目录。
**改造**：加 `agents/security-auditor.md` 等，配 Task 工具调用。

### S-2 resume 会话 token 优化
**状态**：❌ 未开始
**原因**：spec § 5.7 v0.2 计划项。当前每次 SessionStart 都注入 baseline，resume 会话重复。
**改造**：检测 `source === 'resume'` + transcript 已有 baseline → 跳过注入。

### S-3 `oms dashboard` 本地命令
**状态**：❌ 未开始
**原因**：让开发者看自己的 DOP 数据（session 数、命令频率、代码量趋势）。
**改造**：调 DOP query API（需 DOP 团队提供）+ 终端表格输出。

### S-4 OpenCode 支持
**状态**：❌ 未开始
**原因**：spec § 12 v0.3 计划。企业若引入 OpenCode 需要双端产物。
**改造**：写 build 脚本生成 `.opencode/` 目录结构。

### S-5 multi-language baseline
**状态**：❌ 未开始
**原因**：跨国团队。当前 baseline 全中文，无 `content/enterprise-baseline.en.md`。
**改造**：`content/enterprise-baseline.zh.md` + `.en.md`，按 user config 选。

## 📊 推荐执行顺序（按价值排序）

价值排序标准：**交付承诺 > 安全/可靠 > 可用性 > 数据质量 > 可维护性 > 扩展**

### 第 1 梯队：核心承诺兑现（缺了产品承诺就落空）

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 1 | S-6.4 CI openspec validate | 1 小时 | ❌ | spec 保鲜的最后一道防线——没有它 drift 无从检测 |
| 2 | S-6.5 保鲜文档化 | 2 小时 | ❌ | 新人唯一理解保鲜机制的入口——没有文档等于没做 |
| 3 | F-2 userConfig 替代手改 config | 半天 | ❌ | 企业用户不改 config.json 就调不了 DOP/遥测——当前零配置不可用 |

### 第 2 梯队：生产可用性（mock → 真实环境）

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 4 | N-3 真 DOP endpoint 联调 | 待协调 | ❌ | 所有 DOP 测试都是 mock——遥测核心功能从未验证 |
| 5 | N-8 真 dop CLI 命令语法验证 | 半天 | ❌ | 5 个 SDD 命令的 dop 调用语法从未确认——可能全错 |
| 6 | N-7 真 gh CLI 集成验证 | 半天 | ❌ | /sdd-spec/review 依赖 gh CLI——从未端到端验证 |

### 第 3 梯队：数据完整性与健壮性

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 7 | F-5 SessionEnd 不可靠补传 | 中等 | ❌ | 用户关终端 → 孤儿 session.start 永远留在 DOP——数据污染 |
| 8 | F-3 files_touched 上报 session.end | 半天 | 🔄 | 增量记录已写但不上报——白做工，补齐 DOP 数据画像 |
| 9 | N-4 SessionStart hook 副作用验证 | 小 | ❌ | session meta / DOP 上报是否真在工作——从未真机验证 |

### 第 4 梯队：数据质量与 bug 修复

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 10 | F-4 slash_commands 去重 | 小 | ❌ | `['sdd-plan','sdd-plan','sdd-plan']` 污染 DOP 统计 |
| 11 | F-6 checkNodeVersion NaN bug | 小 | ❌ | API bug——当前调用方碰巧不受影响但随时可能踩 |
| 12 | C-6 NEED_LOGIN 测试 regex 收紧 | 小 | ❌ | 测试太宽——NEED_LOGIN 和 ERROR 都能匹配，假通过 |

### 第 5 梯队：代码卫生（v0.2 发布前执行）

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 13 | C-4 isIamInPath await 统一 | 小 | 🔄 | 风格不一致但无害——发布前顺手修 |
| 14 | C-3 toStderr 参数清理 | 小 | ❌ | dead API——发布前顺手删 |
| 15 | C-2 删 plan-mandated 死代码 | 1 小时 | ❌ | 发布前清理 |
| 16 | C-1 删 legacy cleanup 代码 | 30 分钟 | ⚠️ | 给用户 1 个版本周期后删 |

### 第 6 梯队：持续监控与过程改进

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 17 | N-1 监控 #16538 修复 | 持续 | ⏳ | 修复后回归纯 plugin 方案 |
| 18 | N-2 删冗余 additionalContext 输出 | 等 N-1 | ⏳ | defense-in-depth——bug 修了自动恢复 |
| 19 | N-5 plugin cache 刷新文档 | 小 | ❌ | 开发体验改善 |
| 20 | N-6 多插件共存文档 | 小 | ❌ | 企业环境常见问题 |
| 21 | N-9 superpowers 模板 bug 反馈 | 小 | ❌ | 上游协作——影响 writing-plans 质量 |
| 22 | N-10 alpha 试用反馈模板 | 小 | ❌ | 收集结构化反馈 |

### 第 7 梯队：战略扩展（v0.3+）

| # | 项 | 估时 | 状态 | 为什么优先 |
|---|----|------|------|-----------|
| 23 | C-5 token lint tiktoken | 中等 | ❌ | 精确 token 计数——nice-to-have |
| 24 | S-1 显式 subagents | 2 天 | ❌ | 企业可能需要——当前无实际需求 |
| 25 | S-2 resume 会话 token 优化 | 中等 | ❌ | 节约 token——但 baseline 很小，收益有限 |
| 26 | S-3 `oms dashboard` 本地命令 | 中等 | ❌ | 需 DOP 团队提供 API |
| 27 | S-4 OpenCode 支持 | 中等 | ❌ | 需企业引入 OpenCode 才有需求 |
| 28 | S-5 multi-language baseline | 中等 | ❌ | 跨国团队需求——当前团队全中文 |

---

**v0.2 最小可用范围**：第 1 梯队（S-6.4 + S-6.5 + F-2），约 1.5 天。
**v0.2 完整范围**：第 1~4 梯队（+ N-3/8/7 + F-5/3 + F-4/6/C-6），约 4 天。
**v0.3 时机**：N-1（#16538）修复 → 删 CLAUDE.md 注入路径回归纯 plugin；或战略扩展项有明确需求时。

---

## 🆕 v0.1 发布过程新增 backlog（B-1 排查期间发现）

### N-1 监控 Claude Code bug #16538 修复进度
**状态**：⏳ 持续监控中
**背景**：plugin SessionStart hook additionalContext 被静默 drop，当前用 `~/.claude/CLAUDE.md` 绕过。
**行动**：
- 关注 https://github.com/anthropics/claude-code/issues/16538
- 关注 https://github.com/anthropics/claude-code/issues/11509（local file-based plugins）
- 修复后回归"纯 plugin"方案：删 `install.js` 的 `injectBaseline()` + uninstall 的清理逻辑 + 用户 CLAUDE.md 不再被改

### N-2 session-start.js 删除冗余 additionalContext 输出
**状态**：⏳ 等 N-1 修复后执行
**现状**：`session-start.js` 还在读 baseline + 输出 additionalContext JSON，但 Claude Code 把它静默 drop（#16538）。当前算 defense-in-depth——bug 修了自动恢复工作。
**注意**：等 N-1 监控的 bug 修复后再删。

### N-3 真 DOP endpoint 联调
**状态**：❌ 未开始
**现状**：所有 DOP 测试都是 mock（验证 schema + 入队），从未与真 DOP 后端联通。
**风险**：真 DOP 可能：① 字段名稍不同；② 鉴权方式我们假设错；③ 批量上报支持但没用。
**行动**：协调 DOP 团队做联调测试。如果 schema 不对，紧急发补丁。

### N-4 SessionStart hook 副作用验证
**状态**：❌ 未做
**现状**：CLAUDE.md 注入生效。但 hook 的其他副作用（DOP session.start 上报、session meta 保存）未单独验证。
**行动**：
- 真机会话结束后查 `~/.oh-my-sdd/queue.jsonl`（应有 session.start 事件）
- 查 `~/.oh-my-sdd/sessions/<id>.json` 是否创建
- 查 DOP 后台是否收到事件（依赖 N-3）

### N-5 增加发布版本号到 git tag 策略文档
**状态**：❌ 未开始
**现状**：package.json 有 `version: "0.1.0"`，Claude Code 同版本不刷 cache。改 baseline 等内容必须 `/plugin uninstall + install` 或 `rm cache`。
**行动**：README 加"开发期间如何刷新 plugin cache"说明。

### N-6 多 SessionStart hook 干扰排查
**状态**：❌ 未开始
**背景**：诊断 B-1 时发现用户的 Claude Code 里有别的插件的 `sdd-hook` 命令找不到的报错。企业用户环境复杂，多插件可能互相干扰。
**行动**：文档化"oh-my-sdd 与其他 sdd-* 插件共存"的注意事项。

---

## 🆕 v0.2.0-alpha 演练后新增 backlog（薄包装架构实战暴露）

### N-7 真 gh CLI 集成验证（待联调）
**状态**：❌ 未开始
**背景**：v0.2 命令里假设 `gh issue create` / `gh pr create` / `gh repo edit` 接口。
演练时发现：① 远端 default branch 错设导致 PR base 错；② 网络代理阻断 gh 命令；③ 未验证 GitHub Enterprise 的 `--hostname` 参数需求。
**行动**：
- 真 gh CLI 端到端测 `/sdd-spec` 创建 issue + 分支
- 真 gh CLI 端到端测 `/sdd-review` 创建 PR
- 验证 GitHub Enterprise 兼容性
- 文档化 gh 命令失败时的 fallback

### N-8 真 dop CLI 命令语法验证（待联调）
**状态**：❌ 未开始
**背景**：v0.2 命令里假设：
- `dop change view <id>` 拉详情（spec 用）
- `dop change update <id> --status <X>` 标记状态（spec/plan/review 用）
**真实 dop CLI 接口未确认**。
**行动**：
- 跑 `dop --help` + `dop change --help` 确认实际接口
- 更新 5 个命令里的 dop 调用语法
- 测试每种状态（spec-in-progress / plan-ready / review-done）

### N-9 superpowers:writing-plans 模板 bug 反馈（外部协作）
**状态**：❌ 未开始
**背景**：用户演练 Ring 2 时发现 plan 模板 3 个 bug：① grep pattern 问题；② 验证逻辑问题；③ baseline commit 规划缺失。已在演练项目的 RETRO.md 记录。
**行动**：
- 提取 RETRO.md 具体内容
- 起草 issue 报告（https://github.com/obra/superpowers）
- 跟踪修复进度，v0.3 升级 superpowers 版本约束

### N-10 v0.2.0-alpha 试用反馈收集机制
**状态**：❌ 未开始
**背景**：tag v0.2.0-alpha 后需要小范围试用 + 收集反馈。当前无标准反馈模板。
**行动**：
- 写 `docs/release/alpha-trial-report-template.md`
- 反馈汇总到 `docs/roadmap/v0.3-backlog.md`

### N-11 /sdd-task "可选环节"语义未充分明示
**状态**：✅ 已修复
**现状**：`skills/sdd-task/SKILL.md` 标题为 `/sdd-task —— SDD 第 3 环：任务细化（可选环节）`，description 明示"SDD Ring 3 可选环节——粒度合适时直接用 /sdd-apply 即可，无需经过本环。"

---

## 🚨 P0 战略级缺口：openspec 保鲜机制未完全落地（v0.2 头号议题）

### S-6 openspec 项目 specs 保鲜机制（修复进度 60%）
**核心问题**：用户引入 openspec 是为了 **spec 资产保鲜**——让 `openspec/specs/` 成为权威的、永远反映系统现状的活文档。

**修复进度**（v0.2.1 alpha）：
- ✅ S-6.1 /sdd-spec 加读现状步骤（步骤 4）
- ✅ S-6.2 spec 文件改 delta 格式（ADDED/MODIFIED/REMOVED）
- ✅ S-6.3 /sdd-review 强制 openspec archive（删 mv fallback）+ merge 验证
- ❌ S-6.4 CI 加 openspec validate（v0.2 待办）
- ❌ S-6.5 文档化"项目 specs 是保鲜资产"（v0.2 待办）

**6 维度评估**（修复后）：
| 维度 | 应该 | 现状 |
|------|------|------|
| 变更工作目录 | 结构化产出 | ✅ 做到 |
| 项目 specs 是权威 | 反映系统现状 | ✅ /sdd-spec/plan 都读 openspec/specs/ |
| Delta 显式化 | 标 ADDED/MODIFIED/REMOVED | ✅ /sdd-spec 强制 delta 格式 |
| 归档时 merge | delta 应用到项目 specs | ✅ 强制 openspec archive，merge 验证 |
| Drift detection | CI 跑 validate | ❌ v0.2 待办（S-6.4） |
| 新变更读现状 | /sdd-spec 先读 openspec/specs/ | ✅ 步骤 4 加读现状 |

**遗留待办**：

#### S-6.4 CI 加 openspec validate
**状态**：❌ 未开始，估时 1 小时
- `.github/workflows/ci.yml` 加步骤：`openspec validate --all --strict`
- 检测 spec 与代码 drift
- 项目级 specs 过期 → CI 红

#### S-6.5 文档：项目 specs 是保鲜资产
**状态**：❌ 未开始，估时 2 小时
- README 加"openspec/specs/ 是项目权威 specs，反映系统现状"
- /sdd-spec 命令开头明示"先读 openspec/specs/ 再写 delta"
- 写 onboarding 文档：新人怎么读 openspec/specs/ 了解系统

**合计剩余估时**：约 3 小时。

**为什么 P0**：没有 S-6.4 + S-6.5，整个 plugin 的"spec 资产保鲜"承诺缺少完整闭环。CI 验证是保鲜的最后一道防线，文档是新人理解保鲜机制的唯一入口。
